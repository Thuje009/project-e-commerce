import React from 'react';

type Props = {
  params: {
    id: string;
  };
}

const ViweProduct: React.FC<Props> = ({ params }) => {
  const { id } = params;
  return (
      <section>
        <div>
          
        </div>
      </section>
  );
};

export default ViweProduct;
